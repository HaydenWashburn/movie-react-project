<!-- Refactor fetch call to be in a promise.All() method so that multiple promises can be handled at once-->

<!-- Create a route that when a movie is selected the user will see the poster for the film as well as a description of the film. (singleFilmPage) -->

<!-- Create a function that loops through the API and generates a random list of movies (list of 12 movies max) -->

